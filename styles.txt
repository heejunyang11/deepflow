@import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;700&display=swap');

body { font-family: 'Inter', sans-serif; }

/* ナビバー */
.navbar {
  background-color: #1E293B !important;
  border-bottom: 1px solid rgba(255,255,255,0.06) !important;
}
.navbar-brand {
  color: #38BDF8 !important;
  letter-spacing: 0.06em !important;
}
.nav-link { color: #94A3B8 !important; }
.nav-link:hover { color: #38BDF8 !important; }

/* ヒーロー */
.quarto-title-banner {
  background-color: #0F172A !important;
  background-image:
    linear-gradient(rgba(56,189,248,0.04) 1px, transparent 1px),
    linear-gradient(90deg, rgba(56,189,248,0.04) 1px, transparent 1px) !important;
  background-size: 40px 40px !important;
  border-bottom: 1px solid rgba(56,189,248,0.12) !important;
  padding: 4rem 0 3rem !important;
}
.quarto-title-banner .title {
  color: #F1F5F9 !important;
  font-size: 3rem !important;
  font-weight: 500 !important;
  letter-spacing: 0.02em !important;
}
.quarto-title-banner .description {
  color: #64748B !important;
  letter-spacing: 0.08em !important;
  text-transform: uppercase !important;
  font-size: 0.85rem !important;
}

/* ページ全体の背景 */
body, .page-layout-full {
  background-color: #0F172A !important;
}

/* 記事カード */
.quarto-post {
  background: #1E293B !important;
  border: 0.5px solid rgba(255,255,255,0.06) !important;
  border-left: 3px solid #38BDF8 !important;
  border-radius: 12px !important;
  transition: border-color 0.2s !important;
  overflow: hidden !important;
  padding: 1rem !important;
}
.quarto-post:hover {
  border-color: rgba(56,189,248,0.4) !important;
}
.quarto-post .title { color: #E2E8F0 !important; }
.quarto-post .description { color: #64748B !important; }
.quarto-post .listing-date { color: #475569 !important; }

/* カテゴリバッジ */
.listing-category {
  background: rgba(56,189,248,0.06) !important;
  border: 1px solid rgba(56, 189, 248, 0.18) !important;
  color: #7DD3FC !important;
  border-radius: 999px !important;
  font-size: 10px !important;
  letter-spacing: 0.04em !important;
}

/* サイドバー */
.quarto-listing-category-title { color: #38BDF8 !important; font-size: 10px !important; letter-spacing: 0.12em !important; }
.quarto-listing-category a { color: #64748B !important; }
.quarto-listing-category a:hover { color: #38BDF8 !important; }

/* フッター */
.nav-footer {
  background-color: #020617 !important;
  border-top: 1px solid rgba(56,189,248,0.08) !important;
  color: #334155 !important;
}

/* コードブロック */
pre {
  background: #020617 !important;
  border: 1px solid rgba(56,189,248,0.15) !important;
  border-radius: 8px !important;
}

/* スクロールバー */
::-webkit-scrollbar { width: 6px; }
::-webkit-scrollbar-track { background: #0F172A; }
::-webkit-scrollbar-thumb { background: #334155; border-radius: 3px; }
::-webkit-scrollbar-thumb:hover { background: #38BDF8; }

/* 見出し・テキスト全般 */
h1, h2, h3, h4 { color: #E2E8F0 !important; }
p, li { color: #94A3B8; }
a { color: #38BDF8 !important; }
a:hover { color: #7DD3FC !important; }
